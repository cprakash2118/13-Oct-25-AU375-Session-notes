---
ansible-navigator:
 execution-environment:
   container-engine: podman
   enabled: True
  #enabled: Yes
  #enabled: 1
   image: aap.lab.example.com/ee-supported-rhel8:latest
   pull:
     policy: missing
 playbook-artifact:
   enable: False
 mode: stdout 
